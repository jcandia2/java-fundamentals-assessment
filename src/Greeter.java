public interface Greeter {
//    Create an interface named Greeter
//    The interface should specify a method named sayHello() that accepts no arguments and returns a string.
//    Change your Person class so that it implements the Greeter interface.
//    The implementation of the sayHello method on the Person class should use the firstName and
//    lastName properties to create a greeting message that is returned.

    String sayHello();
}
